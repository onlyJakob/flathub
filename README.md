# Flatpak for the Norisk Client

## How to build and install

- clone this repo

- Build and locally install the flatpak:
  ```
  flatpak-builder build-dir gg.norisk.noriskclient.yml --user --install --force-clean
  ```
